.icon
  display block
  width 4em
  height 4em
  vertical-align top
  color currentColor

  use
    width 100%
    height 1em
    fill currentColor

  &_logo
    width em(120 / 240 * $baseFontSize)

  &_toplogo
    width em(60 / 120 * $baseFontSize)
